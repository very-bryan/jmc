<div class="p-6">
  <div class="flex items-center gap-3 mb-6">
    <%= link_to "← 목록", admin_announcements_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
    <h1 class="text-2xl font-bold text-gray-900">공지 수정</h1>
  </div>

  <div class="bg-white rounded-lg border border-gray-200 p-6 max-w-2xl">
    <%= render "form", announcement: @announcement %>
  </div>
</div>
